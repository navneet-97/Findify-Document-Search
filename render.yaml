services:
  - type: web
    name: findify-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: findify_db
      - key: CORS_ORIGINS
        value: "*"